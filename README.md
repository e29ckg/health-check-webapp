# 🏥 ระบบเลือกโปรแกรมตรวจสุขภาพประจำปี (Health Checkup Booking System)

Web Application สำหรับให้พนักงานตรวจสอบข้อมูลส่วนตัวและเลือกโปรแกรมตรวจสุขภาพประจำปี พัฒนาโดยใช้ **Google Apps Script (GAS)** เชื่อมต่อฐานข้อมูลกับ **Google Sheets** โดยตรง

![Project Screenshot](https://via.placeholder.com/800x400?text=Health+Checkup+System+Screenshot)
*(คุณสามารถเอารูปหน้าจอโปรแกรมมาใส่ตรงนี้ได้ในภายหลัง)*

## ✨ คุณสมบัติเด่น (Features)

* **Secure Login:** เข้าสู่ระบบด้วยเลขประจำตัวประชาชน (ตรวจสอบข้อมูลจาก Google Sheets)
* **Personal Data:** แสดงข้อมูลส่วนตัว (ชื่อ, นามสกุล, วันเกิด, อายุ) ให้ผู้ใช้ตรวจสอบและแก้ไขได้
* **Dynamic Program Selection:**
    * คำนวณสิทธิ์การเลือกโปรแกรมตามอายุอัตโนมัติ
    * อายุน้อยกว่า 35 ปี เลือกได้เฉพาะโปรแกรม 1-2
    * อายุ 35 ปีขึ้นไป เลือกได้โปรแกรม 3-8
* **Program Details:** แสดงรายละเอียดรายการตรวจและราคาส่วนเกินทันทีที่เลือกโปรแกรม
* **Popup Preview:** ปุ่มกดดูภาพตารางโปรแกรมแบบ Popup (ดึงไฟล์จาก Google Drive) โดยไม่ต้องออกจากหน้าเว็บ
* **Modern UI:** ออกแบบหน้าตาให้ทันสมัย รองรับการใช้งานบนมือถือ (Responsive) ด้วย **Bootstrap 5**
* **Smart Alerts:** แจ้งเตือนสวยงามและเป็นมิตรด้วย **SweetAlert2**

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

* **Frontend:** HTML5, CSS3, JavaScript
* **Frameworks/Libraries:** Bootstrap 5, SweetAlert2, FontAwesome
* **Backend:** Google Apps Script (GAS)
* **Database:** Google Sheets

## ⚙️ วิธีการติดตั้งและใช้งาน (Installation)

### 1. เตรียม Google Sheets
สร้าง Google Sheets ใหม่ โดยแบ่งเป็น 2 แผ่นงาน (Sheets):

* **แผ่นงานที่ 1 (Data):** เก็บข้อมูลพนักงาน
    * `Col A`: เลขประจำตัวประชาชน (Key)
    * `Col B`: คำนำหน้า
    * `Col C`: ชื่อ
    * `Col D`: นามสกุล
    * `Col E`: วันเดือนปีเกิด
    * `Col F`: อายุ
    * `Col G`: เลือกโปรแกรมตรวจสุขภาพ (เก็บผลลัพธ์ที่เลือก)
* **แผ่นงานที่ 2 (Program Details):** เก็บรายละเอียดรายการตรวจและราคา
    * จัดรูปแบบตารางตาม Format ที่กำหนด (แถวรายการตรวจ, คอลัมน์ราคา)

### 2. ติดตั้ง Script
1.  ที่ Google Sheets ไปที่เมนู **ส่วนขยาย (Extensions)** > **Apps Script**
2.  สร้างไฟล์ `Code.gs` และวางโค้ดฝั่ง Server
3.  สร้างไฟล์ `Index.html` และวางโค้ดหน้าบ้าน (Frontend)

### 3. การตั้งค่าสิทธิ์รูปภาพ (สำคัญ)
หากต้องการให้ฟังก์ชันดูรายละเอียดรูปภาพทำงานได้:
1.  อัปโหลดไฟล์ภาพตารางโปรแกรมลง Google Drive
2.  แชร์ไฟล์เป็น **"ทุกคนที่มีลิงก์ (Anyone with the link)"**
3.  นำ ID ของไฟล์มาใส่ในโค้ด JavaScript ฟังก์ชัน `showProgramImage()`

### 4. การ Deploy (ทำให้ใช้งานได้)
1.  กดปุ่ม **การทำให้ใช้งานได้ (Deploy)** > **การทำให้ใช้งานได้รายการใหม่ (New deployment)**
2.  เลือกประเภท: **เว็บแอป (Web app)**
3.  การตั้งค่า:
    * Execute as: **ฉัน (Me)**
    * Who has access: **ทุกคน (Anyone)**
4.  คัดลอก URL ที่ได้ไปใช้งานได้เลย

## 📝 วิธีการใช้งาน (Usage)

1.  ผู้ใช้งานเข้าผ่านลิงก์ Web App
2.  กรอกเลขประจำตัวประชาชนเพื่อเข้าสู่ระบบ
3.  ตรวจสอบข้อมูลส่วนตัว (แก้ไขวันเกิด/อายุ หากไม่ถูกต้อง)
4.  กดปุ่ม **"ดูรายละเอียด"** เพื่อดูตารางเปรียบเทียบโปรแกรม
5.  เลือกโปรแกรมที่ต้องการ (ระบบจะกรองโปรแกรมตามอายุให้)
6.  กด **"บันทึกข้อมูล"**

## 🤝 การมีส่วนร่วม (Contributing)
ยินดีต้อนรับการ Pull Request เพื่อปรับปรุงโค้ดหรือเพิ่มฟีเจอร์ใหม่ๆ

---
พัฒนาโดย [ชื่อของคุณ/หน่วยงานของคุณ]
